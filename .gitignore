# dependencies
node_modules/
.pnp/
.pnp.js

# next.js
.next/
out/

# production
build/
dist/

# env files
.env
.env.local
.env.production
.env.development

# debug
npm-debug.log*

# vercel
.vercel/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# scrapers - cloned repos (too large for git)
scrapers/social-analyzer/
scrapers/facebook-scraper/
scrapers/TwitterUserScraper/
scrapers/OpenPlanter/
scrapers/openFEC/

# large data files
congress-members.json

# data-ingestion raw repos (too large)
data-ingestion/phase1/raw/
data-ingestion/jfk-social-results/

# Large scraped data files (synced to Supabase, not needed in git)
data-ingestion/legiscan-votes.json

# Keep FEC results tracked (imported by lib/real-data.ts at build time)
# data-ingestion/jfk-fec-results/ -- DO NOT IGNORE

# Raw FEC / OH SOS itemized contribution pulls — large (10MB-100MB+),
# rebuildable from upstream APIs at any time. The structured crossref
# outputs (data-ingestion/*-roster-matches.json, *.csv) ARE committed.
data-ingestion/*-itemized.json
data-ingestion/audit-tracker.csv
data-ingestion/audit-targets.txt
scrapers/logs/
.vercel
