---
phase: 10
plan: 05
type: execute
wave: 5
depends_on: [10-01]
files_modified:
  - app/social/page.tsx
  - components/SocialFeed.tsx
  - components/USMap.tsx
  - components/ZipMap.tsx
  - app/page.tsx
  - components/TerminalHome.tsx
  - app/hierarchy/page.tsx
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "At 375 px on /, the matrix-rain DOM nodes do NOT exist (gradient backdrop only)"
    - "At 640-1023 px (sm:) on /, matrix rain renders with 12 columns (vs 35) and no text-shadow"
    - "At 1024 px+ (lg:) on /, matrix rain renders with today's 35 columns and full text-shadow"
    - "On any viewport with prefers-reduced-motion: reduce, matrix rain DOES NOT mount (gradient only)"
    - "Cursor blink uses CSS animation (not setInterval); React no longer re-renders every 530ms for cursor"
    - "On / at 375 px, search form stacks vertically (flex-direction column)"
    - "On /social at 375 px, autoRefresh defaults to false; explicit Refresh button is reachable"
    - "On USMap and ZipMap, scrollWheelZoom is false; tap-to-interact overlay shows until first tap"
    - "On /hierarchy at base (<lg:), a top banner reads 'This view is optimised for desktop'; no other layout changes"
  artifacts:
    - path: "components/TerminalHome.tsx"
      provides: "Conditional matrix-rain render branch (gradient at base, 12-col at sm:, 35-col at lg:); CSS cursor blink"
      contains: "matchMedia"
    - path: "app/hierarchy/page.tsx"
      provides: "Desktop-only banner at base; no other mobile layout changes"
      contains: "lg:hidden"
    - path: "components/SocialFeed.tsx"
      provides: "Default autoRefresh=false on base; explicit refresh button; pulse honors prefers-reduced-motion"
      contains: "autoRefresh"
    - path: "components/USMap.tsx"
      provides: "scrollWheelZoom: false; tap-to-interact overlay"
      contains: "scrollWheelZoom"
    - path: "components/ZipMap.tsx"
      provides: "scrollWheelZoom: false; tap-to-interact overlay"
      contains: "scrollWheelZoom"
  key_links:
    - from: "components/TerminalHome.tsx render branch"
      to: "window.matchMedia('(min-width: 640px)') and ('prefers-reduced-motion: reduce')"
      via: "matchMedia checks at mount choose render path"
      pattern: "matchMedia"
    - from: "components/SocialFeed.tsx"
      to: "Refresh button onClick"
      via: "manual fetch trigger replaces setInterval polling on mobile"
      pattern: "autoRefresh"
---

<objective>
Phase E: Homepage rain, social, maps, /hierarchy banner. Cleans up the remaining routes that aren't core deep pages but are linked from the homepage. Implements:
- Matrix rain tiered render branch in `TerminalHome.tsx` per UI-SPEC §9 (gradient at base, 12-col at sm:, 35-col at lg:; off when prefers-reduced-motion)
- Cursor blink via CSS `animation: blink` instead of `setInterval` (AUDIT §1.1 MEDIUM)
- Search form stacks vertically at base
- `SocialFeed.tsx` default `autoRefresh = false` on base + explicit refresh button + pulse honors prefers-reduced-motion
- `USMap.tsx`, `ZipMap.tsx` Leaflet `scrollWheelZoom: false` + tap-to-interact overlay (RISKS §2.3)
- `/hierarchy` banner only — explicitly OUT OF SCOPE for mobile work per D-26

Purpose: AUDIT §1.1 lists matrix rain as the largest perf cost on the homepage (420+ animated DOM nodes, no prefers-reduced-motion guard, CRITICAL). AUDIT §1.5 calls out SocialFeed 30s polling (MEDIUM, battery drain). RISKS §2.3 flags Leaflet iOS Safari touch trap. /hierarchy is deprecated (D-26).

Output: Homepage usable on phones; social page autorefresh-aware; maps don't trap scroll; /hierarchy clearly deprecated.

Autonomous: true — Leaflet behavior on real iPhone is in PR description as a manual QA item (RISKS §2.3) but does not block the autonomous build.

Requirement covered: UI-04 (mobile-responsive layout — homepage + secondary routes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mobile-redesign/10-CONTEXT.md
@.planning/phases/10-mobile-redesign/UI-SPEC.md
@.planning/phases/10-mobile-redesign/PLAN-spec.md
@.planning/phases/10-mobile-redesign/AUDIT.md
@.planning/phases/10-mobile-redesign/RISKS.md
@.planning/phases/10-mobile-redesign/10-01-PLAN.md
@app/page.tsx
@components/TerminalHome.tsx
@components/SocialFeed.tsx
@components/USMap.tsx
@components/ZipMap.tsx
@app/hierarchy/page.tsx
@app/social/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Matrix rain tiered render branch + CSS cursor blink + search form stacking in TerminalHome.tsx</name>
  <files>app/page.tsx, components/TerminalHome.tsx, app/globals-terminal.css</files>
  <read_first>
    - components/TerminalHome.tsx (focus lines 91-95 cursor blink setInterval region, 143-148 search form `flex: '1 1 300px'`, 206-251 matrix-rain 35-column render)
    - app/page.tsx (entry point that mounts TerminalHome)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §9 "Matrix-rain mobile strategy" (verbatim tiered spec)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-19, D-20 (tiered strategy + render-branch not CSS-display)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-04 (prefers-reduced-motion explicit early-return guard in TerminalHome)
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase E" — Files touched row for app/page.tsx + TerminalHome.tsx
    - .planning/phases/10-mobile-redesign/AUDIT.md §1.1 (matrix rain CRITICAL, cursor MEDIUM, search form HIGH)
    - app/globals-terminal.css (existing @keyframes blink? if not, add one)
  </read_first>
  <action>
  Step 1 — Matrix rain tiered render branch in components/TerminalHome.tsx.

  At the top of the component (after hooks setup), add:
  ```
  const [rainTier, setRainTier] = useState<'gradient' | 'reduced' | 'full'>('gradient')

  useEffect(() => {
    if (typeof window === 'undefined') return
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    const mqLg = window.matchMedia('(min-width: 1024px)')
    const mqSm = window.matchMedia('(min-width: 640px)')
    const compute = () => {
      if (reducedMotion) return 'gradient'
      if (mqLg.matches) return 'full'
      if (mqSm.matches) return 'reduced'
      return 'gradient'
    }
    setRainTier(compute())
    const handler = () => setRainTier(compute())
    mqLg.addEventListener('change', handler)
    mqSm.addEventListener('change', handler)
    return () => {
      mqLg.removeEventListener('change', handler)
      mqSm.removeEventListener('change', handler)
    }
  }, [])
  ```

  Render branch in the JSX (replacing the current 35-column block at lines 206-251):
  - When `rainTier === 'gradient'`: render a single static element `<div className="fixed inset-0 z-0 pointer-events-none bg-gradient-to-b from-terminal-green/5 via-black to-black" aria-hidden="true" />`. No DOM nodes per column. No animation. Per D-20: this is a render BRANCH, not `display: none` — when rainTier is `gradient`, the 35-column DOM nodes literally do not exist.
  - When `rainTier === 'reduced'`: render the matrix rain with `columnCount = 12` (vs today's 35). Loop `for (let i = 0; i < 12; i++)` rendering each column. Each column div MUST have a `data-rain-column` attribute for Playwright test instrumentation (per plan 06 Task 2 Step 3). Inside each column, REMOVE `text-shadow` from styles (or set to `'none'` explicitly). Wrap the rain container with an `IntersectionObserver` that pauses animation (`animation-play-state: paused`) when the rain is off-screen. Use a ref + observer in a `useEffect`.
  - When `rainTier === 'full'`: render the existing 35-column rain, but still add `data-rain-column` to each column div (same attribute on both tiers so the Playwright selector works uniformly). Preserve today's text-shadow and animation.

  Hard rule: do NOT use `display: none` to hide rain on base. The DOM nodes must literally not be created (per D-20 / RISKS §1.1, today's 420+ nodes cost memory even when hidden).

  Step 2 — Cursor blink CSS animation.

  Ground truth: TerminalHome.tsx today has a `setInterval` firing approximately every 530 ms that toggles a cursor-visibility boolean state (part of the typewriter/terminal effect). Find the `setInterval` call driving cursor-blink state (approximately 530 ms interval — search for `530` or `setInterval` near the cursor render). Delete that `setInterval`, its matching `useEffect` cleanup, and the `useState` hook holding the cursor-visibility boolean. Pattern-based, not line-bound — if the refactor above shifts line numbers, find by behavior not by line number.

  In `app/globals-terminal.css`, add a keyframe (if `@keyframes blink` doesn't already exist):
  ```
  @keyframes blink { 0%, 50% { opacity: 1; } 50.01%, 100% { opacity: 0; } }
  .cursor-blink { animation: blink 1.06s steps(2, start) infinite; }
  ```

  Replace the cursor element in TerminalHome.tsx with `<span className="cursor-blink">_</span>` (or whatever character is used today — preserve the glyph).

  This eliminates the React re-render every 530 ms (AUDIT §1.1 MEDIUM).

  Step 3 — Search form stacking at lines 143-148.

  Today: `flex: '1 1 300px'` — minimum basis exceeds 375 px viewport per AUDIT.

  Replace the form container with Tailwind classes:
  - Container: `flex flex-col sm:flex-row gap-2 sm:gap-3 w-full`.
  - Input: `flex-1 min-h-[44px] px-3 font-mono text-base`.
  - Submit button: `min-h-[44px] px-4 font-mono uppercase tracking-[0.08em] bg-terminal-green text-black`.

  Remove the `flex: '1 1 300px'` style. At base, input + button stack vertically. At sm:+, they sit on one row with the input flexing.

  Do NOT change the form's submit handler, search routing, or query parameter logic — only the layout.

  Per D-22, do not migrate other inline styles in TerminalHome.tsx. Ticker, state selector, etc. remain unchanged in this PR.
  </action>
  <acceptance_criteria>
    - `grep -q "matchMedia" components/TerminalHome.tsx`
    - `grep -q "rainTier" components/TerminalHome.tsx`
    - `grep -q "prefers-reduced-motion" components/TerminalHome.tsx`
    - `grep -q "min-width: 1024px" components/TerminalHome.tsx` AND `grep -q "min-width: 640px" components/TerminalHome.tsx`
    - `grep -q "data-rain-column" components/TerminalHome.tsx` (test instrumentation hook for plan 06)
    - The cursor-specific setInterval (~530ms cadence) is gone — the matrix rain loop may still use rAF / setInterval; the cursor one specifically is removed. Verify no `useState` named like `cursorVisible` / `blinkOn` remains.
    - `grep -q "@keyframes blink" app/globals-terminal.css` OR `grep -q "cursor-blink" app/globals-terminal.css`
    - `grep -q "cursor-blink" components/TerminalHome.tsx`
    - `grep -q "flex flex-col sm:flex-row" components/TerminalHome.tsx` (search form)
    - `grep -q "flex: '1 1 300px'" components/TerminalHome.tsx` returns no match (replaced)
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "matchMedia" components/TerminalHome.tsx && grep -q "rainTier" components/TerminalHome.tsx && grep -q "prefers-reduced-motion" components/TerminalHome.tsx && grep -q "data-rain-column" components/TerminalHome.tsx && grep -q "cursor-blink" components/TerminalHome.tsx && (grep -q "@keyframes blink" app/globals-terminal.css || grep -q "cursor-blink" app/globals-terminal.css) && ! grep -q "flex: '1 1 300px'" components/TerminalHome.tsx && npx next build</automated>
  </verify>
  <done>Matrix rain renders gradient/reduced/full per viewport with data-rain-column hooks on column nodes; cursor blink is CSS-only (setInterval/useState removed); search form stacks at base; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: SocialFeed default-off polling + Refresh button + maps tap-to-interact + /hierarchy banner + /social title sizes</name>
  <files>components/SocialFeed.tsx, components/USMap.tsx, components/ZipMap.tsx, app/hierarchy/page.tsx, app/social/page.tsx</files>
  <read_first>
    - components/SocialFeed.tsx (focus lines 115-123 setInterval polling, 166-169 pulse animation) — NOTE: plan 10-02 Task 1 extracted DaemonStatusIndicator from SocialFeed; the daemon-status rendering is now via `<DaemonStatusIndicator variant="full" />`. Plan 10-05 only touches autoRefresh polling logic and the feed refresh button — NOT the daemon status (already handled by the extracted component which honors prefers-reduced-motion internally).
    - components/USMap.tsx (focus lines 28-52 Leaflet config — scrollWheelZoom not yet disabled per AUDIT §1.1)
    - components/ZipMap.tsx (similar Leaflet structure to USMap)
    - app/social/page.tsx (line 7 padding inline style)
    - app/hierarchy/page.tsx (any base layout — DO NOT change layout, only add banner)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §3 "Typography scale" Hero/page title row
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §10 row "USMap.tsx, ZipMap.tsx" and row "SocialFeed.tsx"
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-26 (/hierarchy banner only — no other layout changes)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-39 row "SocialFeed.tsx — single responsive; autoRefresh = false default on (base)"
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase E" — Files touched table
    - .planning/phases/10-mobile-redesign/RISKS.md §2.3 (Leaflet iOS Safari mitigation)
  </read_first>
  <action>
  Step 1 — components/SocialFeed.tsx.

  After plan 10-02's refactor, SocialFeed renders `<DaemonStatusIndicator variant="full" />` for daemon state and keeps its own feed polling + LIVE/PAUSED toggle. This plan only touches the FEED polling, not the daemon-status sub-component.

  Find the `setInterval(30_000, ...)` polling block at lines 115-123 (feed-post polling). Wrap it in a viewport check:
  - Add a `useEffect` that runs once on mount: `const isMobile = typeof window !== 'undefined' && window.matchMedia('(max-width: 639px)').matches`. Set local state `autoRefresh` to `!isMobile` (default true on sm:+ and lg:, false on base).
  - The existing `setInterval` for polling: only register the interval when `autoRefresh === true`. Use `useEffect` with `autoRefresh` in dependency array; cleanup removes the interval when autoRefresh transitions to false.

  Add an explicit Refresh button:
  - Render a `<button>` near the top of the feed: `min-h-[44px] px-3 font-mono text-xs uppercase tracking-[0.08em] border border-terminal-border hover:bg-terminal-green/10`. Label: "REFRESH". onClick triggers the same fetch logic the interval calls. Visible at all viewports.

  Pulse animation at lines 166-169 — NOTE: after plan 10-02's refactor, this block should already be inside the extracted DaemonStatusIndicator component (which handles prefers-reduced-motion internally). If any pulse-style `animation: 'pulse 2s infinite'` remains in SocialFeed.tsx (unrelated to daemon status — e.g., on a feed-specific element), wrap it in a `motion-reduce:animate-none` className. The global `@media (prefers-reduced-motion: reduce) { * { animation: none !important }` from Plan 01 already covers this, so an explicit guard is defensive.

  Do NOT change SocialFeed's data shape, post rendering, filter logic, or the DaemonStatusIndicator usage — only polling cadence and refresh button.

  Step 2 — components/USMap.tsx and components/ZipMap.tsx.

  In each Leaflet `MapContainer` (or equivalent) initialization (USMap lines 28-52, ZipMap analogous):
  - Add `scrollWheelZoom={false}` prop. If today's code uses `L.map(...)` directly with options, set `scrollWheelZoom: false` in the options object.

  Add a tap-to-interact overlay:
  - Sibling element rendered absolutely over the map: `<div className="absolute inset-0 z-[400] flex items-center justify-center bg-black/60 cursor-pointer" onClick={() => setInteracted(true)}>...</div>`.
  - Overlay text: `<span className="font-mono text-sm text-terminal-green uppercase tracking-[0.08em]">TAP TO INTERACT</span>`.
  - Use `useState(false)` for `interacted`. When `interacted === true`, the overlay unmounts (`{!interacted && <Overlay />}`).
  - The overlay sits inside a `relative` parent so absolute positioning anchors correctly.
  - The overlay must be above Leaflet's tile layer (Leaflet's default z-index goes up to ~400 for popups; using z-[400] on the overlay matches; if Leaflet popups bleed through, raise to z-[600]).
  - Note: `scrollWheelZoom: false` blocks scroll-wheel zoom on desktop AND on mobile prevents single-finger pan from trapping page scroll. Pinch-zoom and two-finger pan still work normally on mobile.

  Convert any inline-style padding/margin TOUCHED in the maps to Tailwind per D-22 — only the touched lines.

  Step 3 — app/hierarchy/page.tsx.

  Per D-26: NO mobile layout changes. Only add a base-only banner.

  At the top of the page (immediately inside the page's root return):
  ```
  <div className="lg:hidden bg-terminal-amber/10 border-y border-terminal-amber px-3 py-2 text-xs font-mono uppercase tracking-[0.08em] text-terminal-amber text-center">
    This view is optimised for desktop.
  </div>
  ```

  Do NOT touch any other styling, grid columns, or layout in this file. The desktop-only banner is the only change.

  Step 4 — app/social/page.tsx.

  Title at line 11 (today inline `fontSize: '1rem'` per UI-SPEC §3 reference):
  - Replace whatever current title styling exists with `className="text-2xl sm:text-[28px] lg:text-[32px] font-display uppercase tracking-[0.08em] mb-4"` (per UI-SPEC §3 Hero / page title row: 24 → 28 → 32).
  - Section padding at line 7 (`padding: '1.5rem'`): replace with `className="p-3 sm:p-6 lg:p-8"`.

  Do NOT touch SocialFeed mount or data fetching from this file.
  </action>
  <acceptance_criteria>
    - `grep -q "autoRefresh" components/SocialFeed.tsx`
    - `grep -q "matchMedia" components/SocialFeed.tsx`
    - `grep -qi "REFRESH" components/SocialFeed.tsx` (button label)
    - `grep -q "min-h-\[44px\]" components/SocialFeed.tsx` (refresh button tap target)
    - `grep -q "scrollWheelZoom" components/USMap.tsx` AND `grep -q "scrollWheelZoom" components/ZipMap.tsx`
    - `grep -qi "TAP TO INTERACT" components/USMap.tsx` AND `grep -qi "TAP TO INTERACT" components/ZipMap.tsx`
    - `grep -q "lg:hidden" app/hierarchy/page.tsx`
    - `grep -qi "optimised for desktop\|optimized for desktop" app/hierarchy/page.tsx`
    - `grep -q "p-3 sm:p-6 lg:p-8" app/social/page.tsx`
    - `grep -q "text-2xl sm:text-\[28px\] lg:text-\[32px\]" app/social/page.tsx`
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "autoRefresh" components/SocialFeed.tsx && grep -q "scrollWheelZoom" components/USMap.tsx && grep -q "scrollWheelZoom" components/ZipMap.tsx && grep -qi "TAP TO INTERACT" components/USMap.tsx && grep -q "lg:hidden" app/hierarchy/page.tsx && grep -qi "desktop" app/hierarchy/page.tsx && grep -q "p-3 sm:p-6 lg:p-8" app/social/page.tsx && npx next build</automated>
  </verify>
  <done>SocialFeed defaults autoRefresh=false on base; explicit refresh button; pulse honors reduced-motion; maps tap-to-interact; /hierarchy banner shows at base only; /social padding/title standardized; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← matchMedia (`prefers-reduced-motion`, viewport size) | Browser-controlled, trustworthy. |
| Leaflet ← tile CDN (CartoDB) | Same as today; tiles loaded from CartoDB CDN. |
| SocialFeed ← API (`/api/social-posts`, `/api/daemon-status`) | Same as today; refresh button forwards to existing endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-18 | DoS | Matrix-rain on mobile | mitigate | Render branch eliminates DOM nodes at base (D-20). 12-col reduced rain at sm: with IntersectionObserver pause when off-screen. Today's 35-col cost reduced by ~98% on mobile. |
| T-10-19 | DoS | SocialFeed 30s polling on mobile | mitigate | Default autoRefresh=false on base; explicit refresh button. Battery drain reduced. |
| T-10-20 | DoS | Leaflet single-finger map drag traps page scroll | mitigate | scrollWheelZoom: false + tap-to-interact overlay (RISKS §2.3). Verified on real iPhone in PR description QA. |
| T-10-21 | Information Disclosure | /hierarchy banner | accept | Static text; no user input rendered. |
| T-10-22 | Tampering | Refresh button onClick | accept | Same fetch path as today's interval; no new endpoint or auth bypass. |
</threat_model>

<verification>
- `npx next build` passes
- All grep assertions pass
- At 375 px on `/`: gradient backdrop visible, no matrix-rain DOM nodes (`document.querySelectorAll('[data-rain-column]').length === 0`)
- At 640-1023 px (sm:): 12 matrix-rain columns render (selectable via `[data-rain-column]`)
- At 1024 px+ (lg:): 35 matrix-rain columns render (selectable via `[data-rain-column]`; today's behavior preserved)
- DevTools Rendering panel toggle prefers-reduced-motion: reduce → matrix rain disappears at any viewport
- /social: refresh button works; no 30s polling visible in DevTools Network tab on a 375 px viewport
- /hierarchy at 375 px: top banner reads "This view is optimised for desktop"
- /hierarchy at 1280 px: banner not visible (lg:hidden)
- USMap on phone: single-finger swipe scrolls the page, doesn't drag map; tap-to-interact overlay disappears after first tap (RISKS §2.3 manual verification on real iPhone is in PR description)
</verification>

<success_criteria>
From PLAN-spec.md Phase E Acceptance bullets:
- [x] Homepage at 375 px: gradient backdrop (no rain), search form stacks, ticker scrolls
- [x] Social page at 375 px: feed scrolls cleanly, refresh button reachable, no automatic poll
- [x] Map: pinch zoom works inside map; single-finger swipe scrolls the page
- [x] /hierarchy at 375 px: shows a clear "desktop-only" banner; no broken-layout regression beyond what's there today
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-redesign/10-05-SUMMARY.md` documenting:
- Matrix rain tier behavior summary (DOM node counts per viewport tier)
- Whether IntersectionObserver pause was needed at sm: or animation-play-state alone sufficed
- Search Console URL Inspection check (per "specifics" in CONTEXT — manual step) — flag in PR description for the engineer running the merge
- Leaflet iOS Safari touch behavior on real device (RISKS §2.3 — record findings)
- Confirmation that `data-rain-column` is present on both reduced (12-col) and full (35-col) matrix rain renders
</output>
