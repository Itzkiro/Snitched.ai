---
phase: 10
plan: 06
type: execute
wave: 6
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05]
files_modified:
  - app/layout.tsx
  - components/TerminalHeader.tsx
  - components/MobileNavDrawer.tsx
  - components/SearchOverlay.tsx
  - components/ConnectionsGraph.tsx
  - components/TerminalHome.tsx
  - components/PoliticianCard.tsx
  - components/Leaderboard.tsx
  - app/politician/[id]/page.tsx
  - playwright.config.ts
  - tests/e2e/mobile/homepage.spec.ts
  - tests/e2e/mobile/candidates.spec.ts
  - tests/e2e/mobile/officials.spec.ts
  - tests/e2e/mobile/profile.spec.ts
  - tests/e2e/mobile/juicebox.spec.ts
  - tests/e2e/desktop/visual-regression.spec.ts
  - app/globals-terminal.css
  - package.json
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "axe-core via Playwright reports zero CRITICAL or SERIOUS issues on /, /candidates, /officials, /politician/[fixed-test-id], /juicebox"
    - "All 5 mobile e2e specs pass on Vercel preview"
    - "Desktop visual-regression spec passes (no regression at 1280 px against baseline)"
    - "Skip-link <a href=\"#main\">Skip to content</a> is present in app/layout.tsx and all pages wrap content in <main id=\"main\">"
    - "Hamburger button has aria-expanded; active tab has aria-current=\"page\"; drawer has role=\"dialog\" + aria-modal=\"true\""
    - "Focus-visible rings render on every focusable element via global outline rule"
    - "?legacy_nav=1 and ?legacy_graph=1 short-circuits are removed (cleanup)"
    - "playwright.config.ts has two projects: mobile-chrome-pixel-7 (412x915) and desktop-chrome-1280 (1280x800)"
    - "GitHub Actions / Vercel CI runs npx playwright test on PR checks"
    - "All four Playwright selector data-attributes (data-rain-column, data-politician-card, data-vote-filter, data-leaderboard-mobile) resolve to rendered DOM elements"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config with mobile + desktop projects"
      contains: "412"
    - path: "tests/e2e/mobile/homepage.spec.ts"
      provides: "Spec covering nav drawer, ticker, gradient backdrop"
      contains: "spec"
    - path: "tests/e2e/mobile/profile.spec.ts"
      provides: "Spec covering 6 tabs, vote-filter ≥44px, network tab graph mounts"
      contains: "spec"
    - path: "tests/e2e/desktop/visual-regression.spec.ts"
      provides: "1280px snapshot comparison spec for 5 core pages with 1% threshold"
      contains: "toHaveScreenshot"
    - path: "app/layout.tsx"
      provides: "Skip-link + <main id=\"main\">"
      contains: "Skip to content"
  key_links:
    - from: "GitHub Actions / Vercel CI"
      to: "npx playwright test"
      via: "PR check runs both mobile and desktop projects against the preview URL"
      pattern: "playwright test"
---

<objective>
Phase F: Polish, accessibility, Playwright + visual regression. Makes the redesign defensible. The first 5 phases get the site usable; this one makes it correct.

Implements:
- Audit pass for `aria-label`, `role`, `aria-current` on active tab, `aria-expanded` on hamburger across components touched in B-E
- Skip-link `<a href="#main">Skip to content</a>` in app/layout.tsx; all pages wrap content in `<main id="main">`
- Focus-visible rings via global rule (per D-05: 2 px outline in --terminal-amber via :focus-visible)
- `playwright.config.ts` with two projects: `mobile-chrome-pixel-7` (412x915) and `desktop-chrome-1280` (1280x800)
- 5 mobile e2e specs (~30 lines each) under `tests/e2e/mobile/`
- 1 desktop visual-regression spec at `tests/e2e/desktop/visual-regression.spec.ts` (1% threshold, 5 pages)
- @axe-core/playwright a11y check (zero CRITICAL/SERIOUS on 5 pages per D-28)
- Test instrumentation: verify/add all four data-* selector hooks (`data-rain-column`, `data-politician-card`, `data-vote-filter`, `data-leaderboard-mobile`) — some are added in plans 04/05 as part of the same refactor; any still missing are added here to make the Playwright selectors resolve deterministically.
- GitHub Actions / Vercel CI: add `npx playwright test` to PR checks
- Final cleanup of dead CSS rules left over from Phases A-E
- REMOVAL of `?legacy_nav=1` and `?legacy_graph=1` flag short-circuits and the `LegacyHeader` function

Purpose: Phases B-E shipped behind feature flags and on a "looks right" basis. F is the test scaffolding + a11y pass that makes the whole redesign verifiable and removes the rollback escape hatches.

Autonomous: true — all changes are test scaffolding, a11y attribute additions, and CSS cleanup. No new user-facing features.

Requirement covered: UI-04 (mobile-responsive layout — verified via test infrastructure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mobile-redesign/10-CONTEXT.md
@.planning/phases/10-mobile-redesign/UI-SPEC.md
@.planning/phases/10-mobile-redesign/PLAN-spec.md
@.planning/phases/10-mobile-redesign/AUDIT.md
@.planning/phases/10-mobile-redesign/10-01-PLAN.md
@.planning/phases/10-mobile-redesign/10-02-PLAN.md
@.planning/phases/10-mobile-redesign/10-03-PLAN.md
@.planning/phases/10-mobile-redesign/10-04-PLAN.md
@.planning/phases/10-mobile-redesign/10-05-PLAN.md
@app/layout.tsx
@components/TerminalHeader.tsx
@components/MobileNavDrawer.tsx
@components/SearchOverlay.tsx
@components/ConnectionsGraph.tsx
@components/TerminalHome.tsx
@components/PoliticianCard.tsx
@components/Leaderboard.tsx
@app/politician/[id]/page.tsx
@app/globals-terminal.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip-link + main landmark + a11y attribute audit + focus-visible rings + CSS cleanup + flag removal</name>
  <files>app/layout.tsx, components/TerminalHeader.tsx, components/MobileNavDrawer.tsx, components/SearchOverlay.tsx, components/ConnectionsGraph.tsx, app/globals-terminal.css</files>
  <read_first>
    - app/layout.tsx (current shape — confirm where children render to position skip-link and main)
    - components/TerminalHeader.tsx (LegacyHeader function added in plan 02 — to be deleted)
    - components/ConnectionsGraph.tsx (legacy_graph short-circuit added in plan 03 — to be deleted; legacy rollback branch inside `getLayoutOpts` is also removed)
    - components/MobileNavDrawer.tsx (verify role/aria attributes already present from plan 02)
    - components/SearchOverlay.tsx (verify aria-label on close/clear buttons)
    - app/globals-terminal.css (look for dead rules from old breakpoint at 768 px no longer needed; the prefers-reduced-motion global from plan 01 stays)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §11 "Identity preservation" (last brand-checklist before adding focus rings)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-05 (focus rings: 2 px outline in --terminal-amber via :focus-visible)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-27 (a11y scope verbatim)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-30 (legacy flag cleanup in Phase F)
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase F" — Files touched table
  </read_first>
  <action>
  Step 1 — Skip-link + <main id="main"> in app/layout.tsx.

  At the top of the body's children render, add:
  ```
  <a href="#main" className="absolute -top-12 left-2 z-[100] bg-terminal-green text-black px-3 py-2 font-mono text-xs uppercase tracking-[0.08em] focus:top-2 transition-[top] duration-100">
    Skip to content
  </a>
  ```
  This is visually hidden until focused (via the `-top-12` → `focus:top-2` swap), revealing on Tab.

  Then wrap children in `<main id="main">`:
  - If layout currently renders `{children}` directly, change to `<main id="main">{children}</main>`.
  - If a header (TerminalHeader) is rendered inside the layout, place `<main>` after the header, around the children only — do not include the header inside `<main>`.

  Step 2 — A11y attribute audit on components touched in B-E.

  TerminalHeader.tsx:
  - Confirm hamburger has `aria-expanded={drawerOpen}` (added in plan 02). If missing or stale, fix.
  - Add `<nav aria-label="Primary">` wrapper around the desktop nav links list (lg: rendering).

  MobileNavDrawer.tsx:
  - Confirm `role="dialog" aria-modal="true" aria-label="Main navigation"` (added in plan 02).
  - Add `<nav aria-label="Mobile primary">` around the nav-links section inside the drawer.
  - Each nav link should have `aria-current="page"` when its href matches `usePathname()` — wire that in.

  SearchOverlay.tsx:
  - Confirm `aria-label="Close search"` on back button and `aria-label="Clear search"` on clear button (added in plan 02). If missing, add.
  - Add `role="dialog" aria-modal="true" aria-label="Search"` to the overlay root.

  Profile page tab strip (app/politician/[id]/page.tsx):
  - Confirm `aria-current="page"` on active tab (added in plan 03). If missing, add.
  - Wrap the tab strip in `<nav aria-label="Profile sections">` (or similar role/label).
  - NOTE: this file is in scope for plan 03's specific line ranges. For Phase F, it is permitted to add aria-* attributes to elements in plan 03's range as long as no other styling changes are introduced.

  ConnectionsGraph.tsx:
  - Add `role="region" aria-label="Politician connections graph"` to the Cytoscape mount container.

  Step 3 — Focus-visible rings global rule.

  In app/globals-terminal.css, add (or update if already present from plan 01):
  ```
  *:focus-visible {
    outline: 2px solid var(--terminal-amber);
    outline-offset: 2px;
  }
  ```
  Per D-05: 2 px outline in --terminal-amber via :focus-visible (mouse users do not see it; keyboard users do).

  Verify existing CSS does not have any `outline: none` rules that override this. If found, replace with `outline: none` only when paired with an alternative focus indicator.

  Step 4 — Remove `?legacy_nav=1` flag and LegacyHeader from TerminalHeader.tsx.

  - Delete the URLSearchParams check at the top of TerminalHeader.tsx that returns the LegacyHeader.
  - Delete the local `function LegacyHeader()` definition.
  - Delete the `// TODO: remove with ?legacy_nav=1 flag cleanup in Phase F` comment.
  - Verify there are no other references to `legacy_nav` anywhere in the codebase via grep.

  Step 5 — Remove `?legacy_graph=1` flag from ConnectionsGraph.tsx.

  - Delete the `isLegacyGraph` declaration.
  - Delete the `if (isLegacyGraph) { return {...legacy config...} }` branch inside `getLayoutOpts`'s `case 'fcose':` block (plan 10-03 Task 2 Step 1 enumerated every fcose option including `animate: true`, `animationDuration: 500`, `randomize: true`, `quality: 'proof'`, `nodeSeparation: 80`, `idealEdgeLength: 150`, `nodeRepulsion: () => 20000`, `edgeElasticity: () => 0.45`, `gravity: 0.2`, `gravityRange: 3.8`, `numIter: 2500`). Keep only the non-legacy branch:
    ```
    case 'fcose':
    default:
      return {
        name: 'fcose',
        animate: (isMobile || prefersReducedMotion) ? false : true,
        animationDuration: 500,
        randomize: false,
        quality: 'proof',
        nodeSeparation: 80,
        idealEdgeLength: 150,
        nodeRepulsion: () => 20000,
        edgeElasticity: () => 0.45,
        gravity: 0.2,
        gravityRange: 3.8,
        numIter: isMobile ? 600 : 2500,
      } as unknown as cytoscape.LayoutOptions;
    ```
  - Verify no other references to `legacy_graph` in the codebase via grep.

  Step 6 — CSS cleanup in globals-terminal.css.

  Identify rules made dead by Phases A-E. Specifically:
  - The 768 px-only breakpoint block (`@media (max-width: 768px)`) — if it still exists with nav-related rules, the nav rules are dead (drawer replaces them in plan 02). Other rules in the same media query (e.g., font sizing for older patterns) — REVIEW each rule individually:
    - If the rule targets a Tailwind-migrated component, delete it.
    - If the rule targets a pre-Tailwind element still in the codebase (e.g., raw `.terminal-section` styles untouched by phases B-E), KEEP it.
  - The `.data-grid` rule at line 196 (per AUDIT) using `minmax(280px, 1fr)` — KEEP (still used by /officials data-grid; PoliticianCard fix in plan 04 resolves the overflow).
  - The `pulse` keyframe — KEEP (used by DaemonStatusIndicator, now wrapped in motion-reduce guard).
  - The mobile-nav `overflow-x: auto` rule — should already be removed in plan 02. Verify not re-added.
  - The `.candidates` stats CSS override at line 590 (per AUDIT — `minmax(100px, 1fr)`) — DELETE if its parent grid is now Tailwind-managed in plan 04.

  Document each rule deleted in the SUMMARY for traceability.
  </action>
  <acceptance_criteria>
    - `grep -q 'Skip to content' app/layout.tsx`
    - `grep -q 'href="#main"' app/layout.tsx`
    - `grep -q '<main id="main"' app/layout.tsx` (or equivalent JSX form)
    - `grep -q 'aria-label="Primary"' components/TerminalHeader.tsx`
    - `grep -q 'aria-label="Mobile primary"' components/MobileNavDrawer.tsx`
    - `grep -q 'aria-current' components/MobileNavDrawer.tsx`
    - `grep -q 'role="dialog"' components/SearchOverlay.tsx`
    - `grep -q 'role="region"' components/ConnectionsGraph.tsx`
    - `grep -q ':focus-visible' app/globals-terminal.css`
    - `grep -q -- '--terminal-amber' app/globals-terminal.css` (already present from plan 01; outline rule binds to it)
    - `grep -rq 'legacy_nav' app/ components/ lib/` returns zero matches (flag removed)
    - `grep -rq 'legacy_graph' app/ components/ lib/` returns zero matches (flag removed)
    - `grep -q 'LegacyHeader' components/TerminalHeader.tsx` returns no match
    - `grep -q 'numIter: 2500' components/ConnectionsGraph.tsx` still matches (used as the non-mobile default) — legacy branch removal does not delete the value; verify the code still produces 2500 iterations when isMobile is false
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Skip to content' app/layout.tsx && grep -q 'href="#main"' app/layout.tsx && grep -q '<main id="main"' app/layout.tsx && grep -q ':focus-visible' app/globals-terminal.css && ! grep -rq 'legacy_nav' app/ components/ lib/ && ! grep -rq 'legacy_graph' app/ components/ lib/ && ! grep -q 'LegacyHeader' components/TerminalHeader.tsx && npx next build</automated>
  </verify>
  <done>Skip-link + main landmark + a11y attributes added; focus-visible rings global; legacy flags + LegacyHeader removed; dead CSS cleaned up; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: playwright.config.ts + 5 mobile e2e specs + desktop visual-regression spec + axe-core integration + test instrumentation data-attributes + CI wiring</name>
  <files>playwright.config.ts, tests/e2e/mobile/homepage.spec.ts, tests/e2e/mobile/candidates.spec.ts, tests/e2e/mobile/officials.spec.ts, tests/e2e/mobile/profile.spec.ts, tests/e2e/mobile/juicebox.spec.ts, tests/e2e/desktop/visual-regression.spec.ts, package.json, components/TerminalHome.tsx, components/PoliticianCard.tsx, components/Leaderboard.tsx, app/politician/[id]/page.tsx</files>
  <read_first>
    - package.json (current dev deps; playwright already in package.json per CONTEXT)
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase F" — Files touched table + Testing strategy section
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-33, D-34, D-35, D-36, D-37, D-38 (all testing decisions)
    - Confirm prior plan 04 added `data-politician-card` to PoliticianCard.tsx and `data-leaderboard-mobile` to Leaderboard.tsx, and plan 05 added `data-rain-column` to TerminalHome.tsx matrix-rain columns. Any missing hooks are added in Step 3 below.
  </read_first>
  <action>
  Step 1 — Add @axe-core/playwright dev dep.

  Update package.json devDependencies: add `"@axe-core/playwright": "^4.10.0"` (or latest stable). If `playwright` or `@playwright/test` is missing, add it too: `"@playwright/test": "^1.48.0"`. Run `npm install` is NOT performed by the executor — the executor only edits package.json; the install happens at CI/dev time.

  Step 2 — Create playwright.config.ts (new file at project root).

  ```
  import { defineConfig, devices } from '@playwright/test'

  export default defineConfig({
    testDir: './tests/e2e',
    fullyParallel: true,
    forbidOnly: !!process.env.CI,
    retries: process.env.CI ? 2 : 0,
    reporter: 'html',
    use: {
      baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000',
      trace: 'on-first-retry',
    },
    projects: [
      {
        name: 'mobile-chrome-pixel-7',
        use: { ...devices['Pixel 7'], viewport: { width: 412, height: 915 } },
        testMatch: /mobile\/.*\.spec\.ts/,
      },
      {
        name: 'desktop-chrome-1280',
        use: { browserName: 'chromium', viewport: { width: 1280, height: 800 } },
        testMatch: /desktop\/.*\.spec\.ts/,
      },
    ],
  })
  ```

  Step 3 — Verify and (if missing) ADD test-instrumentation data attributes. The Playwright selectors committed to in Step 4 specs are:
  - `[data-rain-column]` on each matrix-rain column div in `components/TerminalHome.tsx`
  - `[data-politician-card]` on the card root element in `components/PoliticianCard.tsx`
  - `[data-leaderboard-mobile]` on the mobile card-stack wrapper div in `components/Leaderboard.tsx` (the `lg:hidden` branch wrapper)
  - `[data-vote-filter]` on the vote-filter button container wrapper in `app/politician/[id]/page.tsx` at lines 1411-1425

  For each attribute:
  1. Grep the file to check if the attribute already exists (plans 04 and 05 were updated in revision to add data-leaderboard-mobile, data-politician-card, and data-rain-column as part of their scope).
  2. If the attribute is missing, add it in this task — these are test-only edits and per D-22 test-instrumentation additions are acceptable cross-cutting changes.

  Specifically:
  - `components/TerminalHome.tsx`: each matrix-rain column div (inside both the `rainTier === 'reduced'` 12-col branch and the `rainTier === 'full'` 35-col branch) MUST carry `data-rain-column`. The gradient branch renders no column DOM nodes, so `document.querySelectorAll('[data-rain-column]').length` is 0 at base — exactly what the homepage spec asserts.
  - `components/PoliticianCard.tsx`: the card root element MUST carry `data-politician-card`. This identifies each rendered card in candidates/officials/search results.
  - `components/Leaderboard.tsx`: the mobile card-stack wrapper div (the `flex flex-col gap-3 lg:hidden` wrapper from plan 04) MUST carry `data-leaderboard-mobile`. The desktop grid branch does NOT carry this attribute so the juicebox spec only counts it at mobile viewport.
  - `app/politician/[id]/page.tsx`: the vote-filter button container at lines 1411-1425 (the wrapper holding the 6 filter buttons) MUST carry `data-vote-filter`. Individual buttons inherit via the parent selector `[data-vote-filter] button`.

  Acceptance greps for this step are enumerated in the acceptance_criteria block below.

  Step 4 — Create the 5 mobile specs. Each ~30 lines targeting one critical flow per D-34. The fixed-test-id for /politician/ should be a known-stable politician (Claude's discretion; suggest using one already seeded in the codebase — leave the actual id as a constant `TEST_POLITICIAN_ID` at the top of profile.spec.ts; the engineer running the tests can override via `process.env.TEST_POLITICIAN_ID`).

  tests/e2e/mobile/homepage.spec.ts:
  ```
  import { test, expect } from '@playwright/test'
  import AxeBuilder from '@axe-core/playwright'

  test.describe('homepage at 412 px', () => {
    test('nav drawer opens', async ({ page }) => {
      await page.goto('/')
      const hamburger = page.getByRole('button', { name: /open navigation/i })
      await expect(hamburger).toBeVisible()
      await hamburger.click()
      await expect(page.getByRole('dialog', { name: /main navigation/i })).toBeVisible()
    })
    test('ticker visible', async ({ page }) => {
      await page.goto('/')
      // ticker: robust role/text selector
    })
    test('gradient backdrop confirmed (no matrix-rain DOM nodes at base)', async ({ page }) => {
      await page.goto('/')
      const rainNodes = await page.locator('[data-rain-column]').count()
      expect(rainNodes).toBe(0)
    })
    test('no a11y critical/serious', async ({ page }) => {
      await page.goto('/')
      const results = await new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze()
      const critical = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious')
      expect(critical).toEqual([])
    })
  })
  ```

  tests/e2e/mobile/candidates.spec.ts:
  ```
  import { test, expect } from '@playwright/test'
  import AxeBuilder from '@axe-core/playwright'

  test('candidates: at least 1 politician card visible, no horizontal page scroll', async ({ page }) => {
    await page.goto('/candidates')
    const cards = page.locator('[data-politician-card]')
    expect(await cards.count()).toBeGreaterThan(0)
    const horizontal = await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth)
    expect(horizontal).toBe(true)
  })

  test('candidates: no a11y critical/serious', async ({ page }) => {
    await page.goto('/candidates')
    const results = await new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze()
    const critical = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious')
    expect(critical).toEqual([])
  })
  ```

  tests/e2e/mobile/officials.spec.ts: same pattern as candidates, against `/officials`, using `[data-politician-card]`.

  tests/e2e/mobile/profile.spec.ts:
  ```
  import { test, expect } from '@playwright/test'
  import AxeBuilder from '@axe-core/playwright'

  const TEST_POLITICIAN_ID = process.env.TEST_POLITICIAN_ID || 'fl-house-2026-aaron-baker'

  test('profile: all 6 tabs render their content when tapped', async ({ page }) => {
    await page.goto(`/politician/${TEST_POLITICIAN_ID}`)
    const tabs = ['overview','votes','funding','network','social','legal']
    for (const t of tabs) {
      await page.getByRole('button', { name: new RegExp(t, 'i') }).first().click()
      await expect(page).toHaveURL(new RegExp(`#${t}$`))
    }
  })

  test('profile: vote-filter buttons are at least 44 px tall', async ({ page }) => {
    await page.goto(`/politician/${TEST_POLITICIAN_ID}#votes`)
    const filterButtons = page.locator('[data-vote-filter] button')
    const count = await filterButtons.count()
    expect(count).toBeGreaterThan(0)
    for (let i = 0; i < count; i++) {
      const box = await filterButtons.nth(i).boundingBox()
      expect(box?.height ?? 0).toBeGreaterThanOrEqual(44)
    }
  })

  test('profile: network tab graph mounts', async ({ page }) => {
    await page.goto(`/politician/${TEST_POLITICIAN_ID}#network`)
    // Cytoscape mounts a canvas; assert presence
    await expect(page.locator('canvas')).toBeVisible({ timeout: 10000 })
  })

  test('profile: no a11y critical/serious', async ({ page }) => {
    await page.goto(`/politician/${TEST_POLITICIAN_ID}`)
    const results = await new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze()
    const critical = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious')
    expect(critical).toEqual([])
  })
  ```

  tests/e2e/mobile/juicebox.spec.ts:
  ```
  import { test, expect } from '@playwright/test'
  import AxeBuilder from '@axe-core/playwright'

  test('juicebox: three card-stack sections render at base', async ({ page }) => {
    await page.goto('/juicebox')
    const stacks = page.locator('[data-leaderboard-mobile]')
    expect(await stacks.count()).toBeGreaterThanOrEqual(3)
  })

  test('juicebox: no horizontal page scroll', async ({ page }) => {
    await page.goto('/juicebox')
    const horizontal = await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth)
    expect(horizontal).toBe(true)
  })

  test('juicebox: no a11y critical/serious', async ({ page }) => {
    await page.goto('/juicebox')
    const results = await new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze()
    const critical = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious')
    expect(critical).toEqual([])
  })
  ```

  All four data-attribute selectors (`data-rain-column`, `data-politician-card`, `data-leaderboard-mobile`, `data-vote-filter`) are committed and must resolve. Step 3 ensures they exist.

  Step 5 — Desktop visual-regression spec at tests/e2e/desktop/visual-regression.spec.ts.

  ```
  import { test, expect } from '@playwright/test'

  const PAGES = [
    { path: '/', name: 'home' },
    { path: '/candidates', name: 'candidates' },
    { path: '/officials', name: 'officials' },
    { path: '/juicebox', name: 'juicebox' },
    { path: `/politician/${process.env.TEST_POLITICIAN_ID || 'fl-house-2026-aaron-baker'}`, name: 'profile' },
  ]

  for (const p of PAGES) {
    test(`desktop visual regression: ${p.name}`, async ({ page }) => {
      await page.goto(p.path)
      await page.waitForLoadState('networkidle')
      await expect(page).toHaveScreenshot(`${p.name}-desktop-1280.png`, { maxDiffPixelRatio: 0.01 })
    })
  }
  ```

  This produces baseline snapshots in `tests/e2e/desktop/visual-regression.spec.ts-snapshots/`. The first run generates them; subsequent runs compare. The 1 % `maxDiffPixelRatio` threshold matches D-35.

  Step 6 — CI wiring.

  Add a `.github/workflows/playwright.yml` (if a `.github/workflows/` directory exists) OR document the Vercel CI step in package.json scripts.

  Add to package.json scripts:
  ```
  "test:e2e": "playwright test",
  "test:e2e:install": "playwright install --with-deps chromium"
  ```

  If `.github/workflows/` does not exist, do not create the workflow file — instead add a note in the SUMMARY that Vercel CI configuration must be done in the Vercel dashboard (post-merge step) to add `npx playwright test` as a check. Per D-38: "Vercel preview URL is the test target; no need to spin up next dev in CI."
  </action>
  <acceptance_criteria>
    - `test -f playwright.config.ts`
    - `grep -q "mobile-chrome-pixel-7" playwright.config.ts`
    - `grep -q "desktop-chrome-1280" playwright.config.ts`
    - `grep -q "412" playwright.config.ts`
    - `grep -q "1280" playwright.config.ts`
    - `test -f tests/e2e/mobile/homepage.spec.ts`
    - `test -f tests/e2e/mobile/candidates.spec.ts`
    - `test -f tests/e2e/mobile/officials.spec.ts`
    - `test -f tests/e2e/mobile/profile.spec.ts`
    - `test -f tests/e2e/mobile/juicebox.spec.ts`
    - `test -f tests/e2e/desktop/visual-regression.spec.ts`
    - `grep -q "@axe-core/playwright" package.json`
    - `grep -q "@playwright/test" package.json`
    - `grep -q "test:e2e" package.json`
    - `grep -q "toHaveScreenshot" tests/e2e/desktop/visual-regression.spec.ts`
    - `grep -q "AxeBuilder" tests/e2e/mobile/homepage.spec.ts`
    - `grep -q "scrollWidth" tests/e2e/mobile/juicebox.spec.ts` OR `tests/e2e/mobile/candidates.spec.ts` (horizontal scroll check)
    - `grep -q "boundingBox" tests/e2e/mobile/profile.spec.ts` (44 px check)
    - Test instrumentation data-attributes resolve (MUST all pass):
      - `grep -q 'data-rain-column' components/TerminalHome.tsx`
      - `grep -q 'data-politician-card' components/PoliticianCard.tsx`
      - `grep -q 'data-leaderboard-mobile' components/Leaderboard.tsx`
      - `grep -q 'data-vote-filter' app/politician/\[id\]/page.tsx`
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f playwright.config.ts && grep -q "412" playwright.config.ts && grep -q "1280" playwright.config.ts && test -f tests/e2e/mobile/homepage.spec.ts && test -f tests/e2e/mobile/profile.spec.ts && test -f tests/e2e/desktop/visual-regression.spec.ts && grep -q "@axe-core/playwright" package.json && grep -q "AxeBuilder" tests/e2e/mobile/homepage.spec.ts && grep -q "boundingBox" tests/e2e/mobile/profile.spec.ts && grep -q 'data-rain-column' components/TerminalHome.tsx && grep -q 'data-politician-card' components/PoliticianCard.tsx && grep -q 'data-leaderboard-mobile' components/Leaderboard.tsx && grep -q 'data-vote-filter' 'app/politician/[id]/page.tsx' && npx next build</automated>
  </verify>
  <done>Playwright config with mobile + desktop projects; 5 mobile specs + 1 desktop visual-regression spec; axe-core integrated; package.json scripts present; all 4 data-* test attributes resolve in the rendered DOM; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ← Vercel preview URL | Playwright fetches preview deployments. Same trust as a real user. |
| axe-core scanner ← rendered DOM | axe-core inspects accessibility tree; no security-relevant access. |
| Snapshot baseline files ← repo | Visual-regression baselines committed to repo; tampering = code review concern, not runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Tampering | playwright.config.ts | accept | Build-time config; not user-reachable. |
| T-10-24 | Information Disclosure | Test snapshots committed to repo | accept | Snapshots are of public-facing pages; no secrets in screenshots. |
| T-10-25 | Repudiation | Skip-link to #main | mitigate | Adds keyboard navigation accessibility per WCAG 2.1; defends against ADA exposure (RISKS §1.2). |
| T-10-26 | DoS | axe-core scan on every PR | accept | Scans 5 pages once per PR; cost negligible. |
| T-10-27 | Tampering | Removed legacy_nav / legacy_graph flags | mitigate | Flag removal eliminates the read-only render switch attack surface added in plans 02-03. |
| T-10-28 | Information Disclosure | data-* test attributes in production HTML | accept | Test attributes (`data-rain-column`, `data-politician-card`, etc.) are non-sensitive DOM markers; no PII or auth state. |
</threat_model>

<verification>
- `npx next build` passes
- All grep assertions pass
- `npx playwright test` runs against a Vercel preview URL (manual at first; CI on subsequent PRs):
  - All 5 mobile specs pass
  - axe-core reports zero CRITICAL/SERIOUS on /, /candidates, /officials, /politician/[fixed-test-id], /juicebox
  - Desktop visual-regression spec passes (or generates baselines on first run)
- Lighthouse mobile (Moto G4 emulation, Slow 4G) on `/`: LCP < 2.5 s, CLS < 0.1, TBT < 300 ms (per D-37; manual run, document in SUMMARY)
- legacy_nav and legacy_graph flags removed from codebase (grep returns zero matches)
- All 4 test-instrumentation data attributes resolve to rendered DOM (grep passes)
</verification>

<success_criteria>
From PLAN-spec.md Phase F Acceptance bullets:
- [x] axe-core run via Playwright reports zero CRITICAL or SERIOUS issues on /, /candidates, /officials, /politician/[representative-id], /juicebox
- [x] All 5 mobile e2e specs green on Vercel preview
- [x] Visual-regression desktop spec green (no regression on 1280 px)
- [x] Lighthouse mobile (Moto G4 emulation, Slow 4G) on /: LCP < 2.5 s, CLS < 0.1, TBT < 300 ms
- [x] All 4 data-* Playwright selectors resolve deterministically (no placeholder selectors)
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-redesign/10-06-SUMMARY.md` documenting:
- Skip-link styling decision (the focus:top-2 transition)
- Which CSS rules were deleted from globals-terminal.css (cleanup audit trail)
- Data-attribute provenance — which were added in prior plans (04/05) vs which needed to be added in this plan's Step 3
- TEST_POLITICIAN_ID chosen and rationale
- Lighthouse mobile actual numbers (LCP, CLS, TBT) measured at PR time
- CI wiring outcome (GitHub Actions file created? Vercel dashboard step needed?)
- Confirmation that legacy_nav and legacy_graph flags + LegacyHeader + legacy rollback branch in getLayoutOpts are all gone
</output>
