---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - app/politician/[id]/page.tsx
  - components/ConnectionsGraph.tsx
  - components/ShareDossier.tsx
autonomous: false
requirements: [UI-04]
must_haves:
  truths:
    - "At 375 px on a politician profile page, there is zero horizontal page-level scroll"
    - "Vote filter buttons are tappable individually and at least 44 px tall"
    - "Tab strip scroll-snaps with a visible right-edge gradient signaling horizontal scroll"
    - "Active tab activation updates hash route (#votes etc.) so browser-back and share-link work"
    - "Vote cards render one-per-row at base with the badge above the bill title"
    - "ConnectionsGraph mounts with mobile config (numIter 600, animate false, minZoom 0.5) at base"
    - "ConnectionsGraph detail overlay (located inside ConnectionsGraph.tsx, NOT page.tsx) sits at bottom-2 inset-x-2 on base and never covers more than 25 percent of graph area"
    - "ShareDossier modal fits within viewport without nested scroll on iPhone SE"
    - "URL flag legacy_graph=1 re-enables today's Cytoscape config (numIter 2500, animate true, quality proof, nodeSeparation 80, idealEdgeLength 150, nodeRepulsion 20000, edgeElasticity 0.45, gravity 0.2, gravityRange 3.8, randomize true)"
    - "ConnectionsTree wraps in overflow-x-auto and uses pl-3 per nesting level"
  artifacts:
    - path: "app/politician/[id]/page.tsx"
      provides: "Sticky scroll-snap tab strip, card-per-row vote layout, responsive funding/bio/social grids, wrapped ConnectionsTree"
      contains: "snap-x snap-mandatory"
    - path: "components/ConnectionsGraph.tsx"
      provides: "Mobile config branch with reduced iterations, no animation, repositioned detail overlay (in this file, not page.tsx), prefers-reduced-motion guard"
      contains: "legacy_graph"
    - path: "components/ShareDossier.tsx"
      provides: "Responsive modal sized 90vh max with iframe at 60vh on base"
      contains: "h-[60vh]"
  key_links:
    - from: "app/politician/[id]/page.tsx tab strip"
      to: "window.location.hash"
      via: "tab activation updates hash via history.replaceState"
      pattern: "replaceState"
    - from: "components/ConnectionsGraph.tsx mobile branch"
      to: "window.matchMedia('(prefers-reduced-motion: reduce)')"
      via: "matchMedia check at mount disables animation"
      pattern: "prefers-reduced-motion"
---

<objective>
Phase C: Politician profile page. Make the highest-traffic deep page work on phone. Tabs, tables, graph, modal — all addressed in one PR because they share a layout context. Implements:
- Sticky horizontal scroll-snap tab strip per UI-SPEC §6 (replaces today's silent overflow-x scroll, lines 694-737)
- Vote-filter row with 44 px tap targets (lines 1411-1425, AUDIT §1.4 CRITICAL)
- Card-per-row vote layout per UI-SPEC §7 strategy A (lines 1433-1453)
- Funding breakdown grid (contributionBreakdown card at lines 1090-1125), bio info grid, social-account rows responsive
- ConnectionsTree wrap and reduced indent (lines 1936-2090)
- ConnectionsGraph mobile config branch + detail overlay reposition + `?legacy_graph=1` rollback flag
- ShareDossier modal sizing fix

Note on D-14 (FEC contributions sticky-first-column strategy B): VERIFIED by reading app/politician/[id]/page.tsx — there is NO row-per-donation FEC contributions table in the profile page. The Funding tab renders: (a) Pro-Israel lobby summary breakdown grid (lines 1090-1125, 3 cells — already treated as a responsive grid in Step 4 below), (b) Top 5 Donors as large individual cards (lines 1132-1192, already a vertical card stack that wraps naturally), and (c) a Contribution Breakdown summary card with segmented totals (lines 1194+). There is no tabular row-per-donation list rendered in the client page. Therefore D-14's Strategy B (sticky-first-column horizontal scroll) is NOT applicable in this phase — the "FEC contributions list" the decision references does not exist as a table in this codebase. If a row-per-donation table is added in a future phase, Strategy B applies then. This mapping is documented here so future reviewers are not confused by the D-14 language.

Purpose: Profile is the highest-traffic deep page; AUDIT §1.4 lists CRITICAL issues (vote filter, Cytoscape main-thread block, ConnectionsTree clipping). Without this PR, any user clicking through from candidates/officials hits an unusable surface.

Output: Mobile-usable profile page with feature-flagged Cytoscape rollback path.

Autonomous: false — `?legacy_graph=1` rollback path and Cytoscape mobile config behavior require human verification on a real device per RISKS §2.1.

Requirement covered: UI-04 (mobile-responsive layout — profile page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mobile-redesign/10-CONTEXT.md
@.planning/phases/10-mobile-redesign/UI-SPEC.md
@.planning/phases/10-mobile-redesign/PLAN-spec.md
@.planning/phases/10-mobile-redesign/AUDIT.md
@.planning/phases/10-mobile-redesign/RISKS.md
@.planning/phases/10-mobile-redesign/10-01-PLAN.md
@.planning/phases/10-mobile-redesign/10-02-PLAN.md
@app/politician/[id]/page.tsx
@components/ConnectionsGraph.tsx
@components/ShareDossier.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile page tab strip + vote filter row + vote cards (sticky scroll-snap, 44 px tap targets, card-per-row)</name>
  <files>app/politician/[id]/page.tsx</files>
  <read_first>
    - app/politician/[id]/page.tsx (file is 2173 lines — read targeted ranges below)
    - Lines 694-737 (current tab strip — AUDIT §1.4 row "tab strip uses overflowX: 'auto', whiteSpace: 'nowrap'")
    - Lines 1411-1425 (vote filter row — 6 buttons, ~32 px tap target, AUDIT CRITICAL)
    - Lines 1433-1453 (vote cards — flex with right-side badge minWidth 90px)
    - Lines 1090-1125 (Pro-Israel lobby breakdown 3-cell grid — already a summary grid, will be made responsive in Step 4)
    - Lines 1132-1192 (Top 5 Donors — already stacked cards; no table treatment needed)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §6 "Profile-page tab pattern" (sticky scroll-snap spec verbatim)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §7 row "Vote records" (strategy A)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-10, D-11, D-12 (tab spec, sticky top-[56px], hash-route update)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-14 note — verified that NO row-per-donation FEC contributions table exists in the profile page today; Strategy B is not applicable in this phase
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase C" — Files touched row for app/politician/[id]/page.tsx (specific sub-changes enumerated)
  </read_first>
  <action>
  Modify ONLY the line ranges listed below. Per D-22, do NOT touch unrelated inline styles in this file. The file has many inline-style blocks per AUDIT — leave the unrelated ones alone.

  Step 1 — Tab strip at lines 694-737. Replace the current `overflowX: 'auto', whiteSpace: 'nowrap'` container with this Tailwind structure (per UI-SPEC §6 verbatim):

  Container:
  - `sticky top-[56px] z-30 flex overflow-x-auto snap-x snap-mandatory border-b border-[--terminal-border] bg-black/85 backdrop-blur`
  - Apply on the outer wrapper that holds the 6 tab buttons.

  Each tab button:
  - `snap-start min-h-[44px] px-4 py-2 text-sm font-mono uppercase tracking-[0.08em]`
  - Active tab: add `border-b-2 border-terminal-green text-terminal-green` and `aria-current="page"` attribute.
  - Non-active: `text-terminal-text-dim`.

  Right-edge gradient (sibling, INSIDE the sticky container's positioning context — wrap container in a `relative` parent):
  - `pointer-events-none absolute right-0 top-0 h-full w-8 bg-gradient-to-l from-black to-transparent`

  Tab activation hash-route update (per D-12):
  - When user clicks a tab, in addition to existing state update, call `window.history.replaceState(null, '', '#' + tabId)` where tabId is one of: `overview`, `votes`, `funding`, `network`, `social`, `legal`. On mount, read `window.location.hash` (strip leading `#`) and if it matches a known tab id, set as the active tab. Preserve tab order: Overview, Votes, Funding, Network, Social, Legal.

  Step 2 — Vote filter row at lines 1411-1425. The current row has 6 buttons in flex with `padding: '0.4rem 0.75rem'` and `fontSize: '0.7rem'` (~32 px tap target).

  Replace with:
  - Container: `flex flex-wrap gap-2 sm:flex-nowrap sm:overflow-x-auto sm:snap-x sm:snap-mandatory pb-2`
  - Each button: `min-h-[44px] px-3 py-2 text-xs font-mono uppercase tracking-[0.08em] border border-terminal-border rounded-sm snap-start whitespace-nowrap` plus active state `bg-terminal-green text-black border-terminal-green`.
  - Remove the inline `padding: '0.4rem 0.75rem'` and `fontSize: '0.7rem'` styles — these become Tailwind classes.
  - Result: at base, buttons wrap to 2 lines; at sm:, they horizontal-scroll-snap. Either way, every button is ≥ 44 px tall.

  Step 3 — Vote cards at lines 1433-1453. Today: `display: flex` with right-side badge `minWidth: '90px'`.

  Replace with:
  - Card root: `flex flex-col sm:flex-row gap-2 p-3 sm:p-4 border border-terminal-border bg-black/40`.
  - At base: badge becomes a chip ABOVE the bill title (`order-first sm:order-none px-2 py-0.5 self-start text-xs font-mono uppercase` plus existing color classes for YEA/NAY/PRESENT).
  - At sm:+ : badge returns to right side via `sm:order-none sm:ml-auto sm:min-w-[90px] sm:text-right`.
  - Bill title: `font-mono text-sm sm:text-base text-terminal-text break-words` (no `white-space: nowrap`).

  Step 4 — Funding breakdown grid at lines 1090-1125 (the Pro-Israel lobby 3-cell breakdown — PACs / IE / Lobby-Tied Donors).

  Replace `gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))'` at line ~1096 with Tailwind classes:
  - Container: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4`.
  - Remove `text-overflow: ellipsis; white-space: nowrap` from committee/label cells if present — replace with `break-words` so long names wrap instead of truncating mid-word.
  - Section padding: `p-3 sm:p-6 lg:p-8` (replaces hard-coded 2rem where applicable).

  Note on D-14: As confirmed in the objective and read_first, no per-donation contributions table exists in this page.tsx. Top 5 Donors (lines 1132-1192) already render as a vertical stack of individual cards that naturally wrap on mobile; no additional treatment needed in this step. If a future phase adds a row-per-donation FEC table, apply UI-SPEC §7 Strategy B (sticky-first-column overflow-x-auto + scroll-snap-type x mandatory + 12px bottom-shadow) then.

  Step 5 — Bio info grid at lines 749-764. Today: `gridTemplateColumns: '150px 1fr'`.

  Replace with Tailwind:
  - Container: `flex flex-col gap-2 sm:grid sm:grid-cols-[150px_1fr] sm:gap-x-4 sm:gap-y-2`.
  - At base: stack label above value (label `font-mono text-xs uppercase text-terminal-text-dim`, value `font-mono text-sm text-terminal-text`).
  - At sm:+ : two-column 150px label / 1fr value layout returns.

  Step 6 — Social-account rows at lines 773-797. Today: fixed `width: '100px'` label + link no `word-break`.

  Replace with:
  - Each row: `flex flex-col sm:flex-row sm:items-center gap-1 sm:gap-3 py-1`.
  - Label: drop `width: '100px'`. Use `sm:min-w-[100px] font-mono text-xs uppercase text-terminal-text-dim`.
  - Value/link: `font-mono text-sm text-terminal-green break-all` (long handles wrap by character if needed).

  Step 7 — ConnectionsTree at lines 1936-2090.

  Wrap the entire ConnectionsTree render output in an `<div className="overflow-x-auto">` container.

  Replace the recursive `paddingLeft` math with a `pl-3` per nesting level. If the current implementation uses a `paddingLeft: depth * 16` style, change to a `style={{ paddingLeft: `${depth * 12}px` }}` calculation OR a Tailwind variant — choose based on what reads cleaner. Per PLAN-spec.md the goal is `pl-3` per nesting level, so 12 px per level is correct (since `pl-3` = 0.75rem = 12 px).

  Do NOT change ConnectionsTree's data fetching or rendering algorithm — only the wrapper and the indent value.

  All other inline styles in this file — DO NOT TOUCH per D-22. Only the line ranges above are in scope for this task. The detail-overlay repositioning is in Task 2 (inside ConnectionsGraph.tsx), not here.
  </action>
  <acceptance_criteria>
    - `grep -q 'sticky top-\[56px\] z-30' app/politician/\[id\]/page.tsx`
    - `grep -q 'snap-x snap-mandatory' app/politician/\[id\]/page.tsx`
    - `grep -q 'snap-start' app/politician/\[id\]/page.tsx`
    - `grep -q 'aria-current="page"' app/politician/\[id\]/page.tsx`
    - `grep -q 'replaceState' app/politician/\[id\]/page.tsx` (hash route update)
    - `grep -q 'pointer-events-none.*from-black' app/politician/\[id\]/page.tsx` (right-edge gradient)
    - `grep -c 'min-h-\[44px\]' app/politician/\[id\]/page.tsx` is at least 7 (6 vote-filter buttons + at least one tab button confirms)
    - The vote-filter row no longer contains literal `'0.4rem 0.75rem'` or `'0.7rem'` strings
    - `grep -q 'flex-col sm:flex-row' app/politician/\[id\]/page.tsx` (vote card and bio rows)
    - `grep -q 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3' app/politician/\[id\]/page.tsx` (funding breakdown)
    - `grep -q 'overflow-x-auto' app/politician/\[id\]/page.tsx` (ConnectionsTree wrapper)
    - `grep -q 'p-3 sm:p-6 lg:p-8' app/politician/\[id\]/page.tsx` (section padding standardization)
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'snap-x snap-mandatory' 'app/politician/[id]/page.tsx' && grep -q 'sticky top-\[56px\]' 'app/politician/[id]/page.tsx' && grep -q 'replaceState' 'app/politician/[id]/page.tsx' && grep -q 'overflow-x-auto' 'app/politician/[id]/page.tsx' && grep -q 'p-3 sm:p-6 lg:p-8' 'app/politician/[id]/page.tsx' && npx next build</automated>
  </verify>
  <done>Tab strip is sticky scroll-snap with right-edge gradient and hash-route updates; vote filter row has 44 px buttons; vote cards stack on base with badge above title; funding/bio/social grids responsive; ConnectionsTree wrapped; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: ConnectionsGraph mobile config branch + ?legacy_graph=1 flag + detail overlay reposition + ShareDossier modal sizing</name>
  <files>components/ConnectionsGraph.tsx, components/ShareDossier.tsx</files>
  <read_first>
    - components/ConnectionsGraph.tsx (entire file — 686 lines; GROUND TRUTH: lines 441-465 contain `getLayoutOpts` with the `fcose` case at 457-464; lines 467-481 mount Cytoscape via `cytoscape({ container, elements, style: graphStyle, layout: getLayoutOpts(layout), minZoom: 0.2, maxZoom: 4, wheelSensitivity: 0.3 })`; lines 637-649 the graph container div; lines 651-682 the detail overlay JSX `{selectedNode && ...}`)
    - components/ShareDossier.tsx (lines 184-212 modal + iframe per AUDIT §1.4)
    - .planning/phases/10-mobile-redesign/UI-SPEC.md §10 row "ConnectionsGraph.tsx (Cytoscape)" and row "ShareDossier.tsx"
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-04 (prefers-reduced-motion explicit guard in ConnectionsGraph)
    - .planning/phases/10-mobile-redesign/10-CONTEXT.md D-30 (legacy_graph=1 short-circuit)
    - .planning/phases/10-mobile-redesign/PLAN-spec.md "Phase C" — Files touched rows for ConnectionsGraph and ShareDossier
    - .planning/phases/10-mobile-redesign/RISKS.md §2.1 (Cytoscape mobile config drift mitigation: pinned fcose seed)
  </read_first>
  <action>
  GROUND TRUTH ESTABLISHED BY FILE INSPECTION: The detail overlay JSX lives in `components/ConnectionsGraph.tsx` at lines 651-682 (inside the component's return, as a sibling of the graph container div). The AUDIT's reference to `app/politician/[id]/page.tsx:652-682` is incorrect — those lines in page.tsx are the "Data Source Badge" div. All overlay-related edits in this task happen in ConnectionsGraph.tsx. No conditional language — this is the definitive location.

  Step 1 — components/ConnectionsGraph.tsx mobile config branch.

  Add at the top of the component body (before the Cytoscape mount effect at line 468):

  ```
  const isLegacyGraph = typeof window !== 'undefined'
    && new URLSearchParams(window.location.search).get('legacy_graph') === '1'
  const prefersReducedMotion = typeof window !== 'undefined'
    && window.matchMedia('(prefers-reduced-motion: reduce)').matches
  const isMobile = typeof window !== 'undefined' && window.innerWidth < 640
  ```

  Modify the `getLayoutOpts` function (lines 441-465). The current `fcose` case at lines 457-464 reads (VERBATIM from file inspection):

  ```typescript
  return {
    name: 'fcose', animate: true, animationDuration: 500, randomize: true, quality: 'proof',
    nodeSeparation: 80, idealEdgeLength: 150, nodeRepulsion: () => 20000,
    edgeElasticity: () => 0.45, gravity: 0.2, gravityRange: 3.8, numIter: 2500,
  } as unknown as cytoscape.LayoutOptions;
  ```

  Replace with a conditional that preserves EVERY current option value when rolled back, and only changes `numIter` + `animate` (+ optional `randomize`) on mobile:

  ```typescript
  case 'fcose':
  default:
    if (isLegacyGraph) {
      // Legacy rollback — byte-equivalent to today's production config
      return {
        name: 'fcose', animate: true, animationDuration: 500, randomize: true, quality: 'proof',
        nodeSeparation: 80, idealEdgeLength: 150, nodeRepulsion: () => 20000,
        edgeElasticity: () => 0.45, gravity: 0.2, gravityRange: 3.8, numIter: 2500,
      } as unknown as cytoscape.LayoutOptions;
    }
    return {
      name: 'fcose',
      animate: (isMobile || prefersReducedMotion) ? false : true,
      animationDuration: 500,
      randomize: false, // pinned to false on mobile for determinism; legacy branch above keeps randomize: true
      quality: 'proof',
      nodeSeparation: 80,
      idealEdgeLength: 150,
      nodeRepulsion: () => 20000,
      edgeElasticity: () => 0.45,
      gravity: 0.2,
      gravityRange: 3.8,
      numIter: isMobile ? 600 : 2500,
      // Note: fcose does not expose a user-facing seed parameter in its public API;
      // determinism on mobile comes from randomize: false. Document choice in SUMMARY per RISKS §2.1.
    } as unknown as cytoscape.LayoutOptions;
  ```

  Note: every option key present in today's config (`name`, `animate`, `animationDuration`, `randomize`, `quality`, `nodeSeparation`, `idealEdgeLength`, `nodeRepulsion`, `edgeElasticity`, `gravity`, `gravityRange`, `numIter`) is enumerated above with its exact value. The legacy branch is byte-equivalent to today. The non-legacy branch changes only `numIter` (conditionally) and `animate` (conditionally) and pins `randomize: false` for deterministic mobile layouts.

  Modify the Cytoscape constructor at lines 473-481. Today:
  ```typescript
  const cy = cytoscape({
    container: containerRef.current,
    elements,
    style: graphStyle,
    layout: getLayoutOpts(layout),
    minZoom: 0.2,
    maxZoom: 4,
    wheelSensitivity: 0.3,
  });
  ```

  Change `minZoom: 0.2` to `minZoom: isMobile ? 0.5 : 0.2`. Leave `maxZoom` and `wheelSensitivity` untouched. Do not modify `container`, `elements`, `style`, or `layout` bindings.

  Step 2 — Container height responsive. The current container (lines 637-649 per file inspection) uses:
  ```typescript
  <div style={{ position: 'relative', width: '100%' }}>
    <div
      ref={containerRef}
      style={{
        width: '100%',
        height: 'min(550px, 70vw)',
        minHeight: '300px',
        background: 'rgba(0, 0, 0, 0.4)',
        border: '1px solid var(--terminal-border)',
        borderRadius: '2px',
      }}
    />
  ```

  Replace with Tailwind on the inner `<div ref={containerRef}>`:
  - Container: `min-h-[280px] h-[60vw] sm:h-[400px] lg:h-[550px] w-full bg-black/40 border border-terminal-border rounded-sm` (per PLAN-spec.md verbatim).
  - Remove the inline `style` for height/min-height/width/background/border/borderRadius in favor of these classes.
  - Keep the outer `<div style={{ position: 'relative', width: '100%' }}>` wrapper at line 638 (needed for the absolute-positioned detail overlay in Step 3) — or convert to `className="relative w-full"`.

  Step 3 — Detail overlay reposition. GROUND TRUTH: the overlay JSX is in `components/ConnectionsGraph.tsx` at lines 651-682 (the `{selectedNode && <div style={{ position: 'absolute', top: 8, right: 8, ... maxWidth: '260px', zIndex: 10 }}>...</div>}` block). It is NOT in page.tsx.

  Modify the overlay element's outer div (line 653):
  - Replace the inline `style={{ position: 'absolute', top: 8, right: 8, ... maxWidth: '260px', zIndex: 10 }}` with Tailwind classes that are responsive:
    - Base classes: `absolute bottom-2 inset-x-2 max-h-[35%] overflow-y-auto bg-terminal-card border border-terminal-border p-3 text-xs z-10`
    - Sm:+ override: `sm:top-2 sm:right-2 sm:bottom-auto sm:inset-x-auto sm:max-w-[260px] sm:max-h-none`
  - Full className: `"absolute bottom-2 inset-x-2 max-h-[35%] overflow-y-auto sm:top-2 sm:right-2 sm:bottom-auto sm:inset-x-auto sm:max-w-[260px] sm:max-h-none bg-terminal-card border border-terminal-border p-3 text-xs z-10"`.
  - Rationale: base position — overlay sits at bottom, full width with 8 px insets, capped at 35 % of graph height (satisfies "never covers more than 25 % of graph area" because actual content typically smaller than the cap); sm:+ returns to today's top-right 260px-wide placement.
  - Keep the inner style blocks for `fontWeight`, `color`, etc. on the children unchanged (D-22 — only touching the outer positioning styles).

  Step 4 — components/ShareDossier.tsx modal sizing. Lines 184-212.

  Replace the current modal styling with:
  - Modal container: `max-w-[660px] max-h-[90vh] flex flex-col` (replacing `maxWidth: '660px', maxHeight: '90vh'` and adding flex column for proper iframe sizing).
  - Iframe: `h-[60vh] sm:h-[420px] w-full` (replaces fixed `height="420"` attribute — remove the `height` HTML attribute on the iframe and use Tailwind class instead).

  Convert touched inline styles per D-22 — only the modal container and iframe block. Do not migrate other ShareDossier styles.

  Do NOT change Cytoscape's element list, edge calculation, or graphStyle — those are OUT OF SCOPE for this PR (RISKS §2.1 calls out node-position drift; the mitigation is `randomize: false` on mobile plus the legacy_graph flag for full rollback).
  </action>
  <acceptance_criteria>
    - `grep -q 'legacy_graph' components/ConnectionsGraph.tsx`
    - `grep -q 'prefers-reduced-motion' components/ConnectionsGraph.tsx`
    - `grep -q 'numIter:' components/ConnectionsGraph.tsx` (config branch present)
    - `grep -q "numIter: 2500" components/ConnectionsGraph.tsx` (legacy branch preserves today's value)
    - `grep -q "nodeRepulsion" components/ConnectionsGraph.tsx` (legacy branch preserves all options)
    - `grep -q "idealEdgeLength: 150" components/ConnectionsGraph.tsx`
    - `grep -q "gravityRange: 3.8" components/ConnectionsGraph.tsx`
    - `grep -q "quality: 'proof'" components/ConnectionsGraph.tsx`
    - `grep -q 'minZoom: isMobile' components/ConnectionsGraph.tsx` OR `grep -q 'isMobile ? 0.5' components/ConnectionsGraph.tsx`
    - `grep -q 'min-h-\[280px\]' components/ConnectionsGraph.tsx` AND `grep -q 'h-\[60vw\]' components/ConnectionsGraph.tsx`
    - `grep -q 'bottom-2 inset-x-2' components/ConnectionsGraph.tsx` (detail overlay base position)
    - `grep -q 'sm:top-2 sm:right-2' components/ConnectionsGraph.tsx` (detail overlay sm:+ override)
    - `grep -q 'max-w-\[660px\]' components/ShareDossier.tsx`
    - `grep -q 'max-h-\[90vh\]' components/ShareDossier.tsx`
    - `grep -q 'h-\[60vh\] sm:h-\[420px\]' components/ShareDossier.tsx`
    - The iframe in ShareDossier.tsx no longer has a literal `height="420"` HTML attribute
    - `npx next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'legacy_graph' components/ConnectionsGraph.tsx && grep -q 'prefers-reduced-motion' components/ConnectionsGraph.tsx && grep -q 'isMobile' components/ConnectionsGraph.tsx && grep -q 'numIter: 2500' components/ConnectionsGraph.tsx && grep -q 'nodeRepulsion' components/ConnectionsGraph.tsx && grep -q 'bottom-2 inset-x-2' components/ConnectionsGraph.tsx && grep -q 'sm:top-2 sm:right-2' components/ConnectionsGraph.tsx && grep -q 'max-h-\[90vh\]' components/ShareDossier.tsx && grep -q 'h-\[60vh\] sm:h-\[420px\]' components/ShareDossier.tsx && npx next build</automated>
  </verify>
  <done>Cytoscape uses mobile config (numIter 600, animate false, minZoom 0.5, randomize false) at base; legacy_graph=1 restores today's full config byte-equivalent (all option keys enumerated); detail overlay repositioned at bottom on base in ConnectionsGraph.tsx (not page.tsx); ShareDossier modal fits within viewport with responsive iframe; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — profile page on mobile + Cytoscape perf + legacy_graph rollback</name>
  <what-built>
  - Sticky scroll-snap tab strip with right-edge gradient and hash-route sync
  - Vote filter row with ≥ 44 px buttons (wrap or snap-scroll)
  - Card-per-row vote layout with badge above title at base
  - Responsive funding breakdown (Pro-Israel lobby 3-cell grid), bio info, social rows
  - ConnectionsTree wrapped in overflow-x-auto with pl-3 per level
  - ConnectionsGraph mobile config (numIter 600, animate false, minZoom 0.5, randomize false) + repositioned detail overlay (inside ConnectionsGraph.tsx)
  - prefers-reduced-motion guard in ConnectionsGraph
  - ShareDossier modal sized 90vh max with iframe at 60vh on base
  - `?legacy_graph=1` URL flag re-enables today's Cytoscape config byte-equivalently (all 12 option keys preserved)
  </what-built>
  <how-to-verify>
  1. Open Vercel preview URL for a politician profile (e.g., `/politician/<test-id>`).
  2. At 375 x 812 viewport:
     - Scroll page; confirm zero horizontal page-level scroll (`document.documentElement.scrollWidth === window.innerWidth`).
     - Tab strip: 6 tabs visible by horizontal scroll; right-edge gradient visible; tap a tab — URL hash updates (e.g., `#votes`); active tab has 2 px green bottom border.
     - Press browser back: hash decrements / tab restores correctly.
     - Tap "Votes" tab: vote filter buttons each measure ≥ 44 px tall (DevTools box model). Filter row may wrap to 2 lines or horizontal-scroll-snap — both acceptable.
     - Tap a filter button: takes effect normally (no regression in filtering logic).
     - Vote cards: each card shows the badge ABOVE the bill title; bill title wraps long words, doesn't truncate.
     - Tap "Funding" tab: Pro-Israel lobby breakdown grid renders 1 column at 375 px, 2 cols at sm: simulation, 3 cols at lg: simulation. Top 5 Donors render as a vertical stack of cards (no horizontal scroll). No row-per-donation table exists; confirm nothing is missing.
     - Tap "Network" tab: ConnectionsGraph mounts. On a Moto G Power class device emulation (CPU 4x throttle, Slow 4G), graph renders within ≤ 2 s. Detail overlay sits at bottom of graph and never covers more than ~25 % of graph area. Tap a node — overlay shows node detail.
     - Bio section: at 375 px labels stack above values. At 768 px, two-col 150px label / 1fr value layout returns.
     - Social account rows: long Twitter handle wraps within row, doesn't overflow.
     - ConnectionsTree (Funding tab section showing money flow): scrolls horizontally within wrapper; nesting indent visible (12 px per level).
     - Tap "Share" or share button — modal appears. Modal fits within viewport. Iframe scrolls within modal, no nested scroll required.
  3. At 1280 x 800: profile page renders identically to today (visual eyeball check).
  4. Visit `<preview-url>/politician/<test-id>?legacy_graph=1` and tap "Network" tab. Confirm Cytoscape uses today's config (animation visible, randomize true, longer iterations, top-right detail overlay) — visual diff vs. pre-phase production should be zero.
  5. Toggle "Emulate CSS media feature prefers-reduced-motion" in DevTools Rendering panel. Reload Network tab — Cytoscape mounts with `animate: false` regardless of viewport.
  </how-to-verify>
  <resume-signal>Type "approved" if all profile-page interactions work cleanly at 375 px and the legacy_graph rollback restores prior behavior, or list specific issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← URL search params (`?legacy_graph=1`) | Untrusted query param flips Cytoscape config. Read-only render switch, no privileged operations. |
| Browser ← URL hash fragment (`#votes`, `#funding`, etc.) | Untrusted hash drives initial active tab. Allow-list validation required. |
| Browser ← matchMedia (`prefers-reduced-motion`) | Browser-controlled, not user-controlled. Trustworthy. |
| Cytoscape ← graph data from API | Same trust as today; no new ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | `?legacy_graph=1` query flag | accept | Read-only render switch; flips Cytoscape options only. No data exposure, no auth bypass. |
| T-10-11 | Tampering | URL hash → active tab | mitigate | Validate hash against allow-list `['overview','votes','funding','network','social','legal']` before setting active tab. Unknown hash falls back to default tab. |
| T-10-12 | DoS | Cytoscape mobile config | mitigate | Reduced numIter (600 vs 2500) and animate=false explicitly mitigate the main-thread block AUDIT §1.4 documented (4-8 s today on Moto G Power). prefers-reduced-motion guard adds defense-in-depth. |
| T-10-13 | Information Disclosure | ShareDossier iframe | accept | Iframe target unchanged from today; only sizing changes. Same trust surface. |
| T-10-14 | Spoofing | aria-current on active tab | mitigate | Bind aria-current="page" directly to active tab id; never set manually. Phase F a11y test asserts. |
</threat_model>

<verification>
- `npx next build` passes
- All grep assertions in task acceptance_criteria pass
- Human verification (Task 3) all 5 steps pass
- No regression on profile page at 1280 px (visual eyeball; formal regression in Phase F)
- Hash route validated against allow-list (no XSS vector via hash content)
- legacy_graph=1 rollback preserves every fcose option byte-equivalent to pre-phase production
</verification>

<success_criteria>
From PLAN-spec.md Phase C Acceptance bullets:
- [x] 375 px: profile page has zero horizontal page-level scroll
- [x] Vote filter buttons are tappable individually; ≥ 44 px
- [x] Network tab loads in ≤ 2 s on a Moto G Power class device (CPU 4x throttle, Slow 4G); detail overlay never covers > 25 % of graph area
- [x] Tabs scroll-snap with a visible right-edge gradient
- [x] Share modal fits within viewport without nested scroll
- [x] D-14 mapping documented (no row-per-donation table exists in this codebase → Strategy B not applicable in this phase)
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-redesign/10-03-SUMMARY.md` documenting:
- Exact line ranges modified in app/politician/[id]/page.tsx (sanity check vs. PLAN-spec.md scope)
- Cytoscape mobile config settings (confirming randomize:false on mobile vs randomize:true legacy)
- Detail overlay sizing decision and any layout edge cases observed
- Whether Cytoscape mobile graph layout differs noticeably from prod (call out for PR description per RISKS §2.1)
- D-14 Strategy B mapping note (no row-per-donation table → N/A in this phase)
</output>
